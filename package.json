{
  "name": "universal-site",
  "version": "1.0.0",
  "description": "Universal static site with Eleventy, ready for n8n automation",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
